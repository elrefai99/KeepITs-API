export enum EUserStatus {
     ACTIVE = "ACTIVE",
     INACTIVE = "INACTIVE",
     BANNED = "BANNED",
     DELETED = "DELETED",
}

export enum EUserRole {
     ADMIN = "ADMIN",
     USER = "USER",
}

export enum ETaskMeetingType {
     NONE = "none",
     GOOGLE = "google",
     TEAMS = "teams",
     CUSTOM = "custom",
}
